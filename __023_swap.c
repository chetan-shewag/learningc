                             //swapped program
                            //here we swapped the valuse    


                            
#include<stdio.h> // sab kuch jo stdio.h file m h => standard input ouptput  - function/feature input output 
#//include<conio.h> // sab kcuh conio.h  import/include swap.c -- console input /output
        void main()
{
    // what happens on compilation -- // swap.c -- compile... a program -- "compiler" int a = 5;  
    // int a = 5;  -- binary  (01) "rules" -- coputer scientist
    // 0000 1010101 0202 -- byte code
    // C is an statically type language
                      int a,b,c; // variable -- koi bi value -- variable is nothing but an memory occupied -- RAM (2byte ) -- a -- //
                      char ch ; 4 byte

                      //clrscr();
                        a=5;
                        b=6;
                        c=a;
                        b=c;
                      printf("\nswapped valu=%d",a,b);
                    //  getch();
}

code --> compiler --> binary form --> if any rule is broken then it gives error --> else the program runs....


void main (int ,int ){

}